{
  "name": "RioPomba",
  "version": "0.1.0",
  "scripts": {
    "start": "npm run server & webpack -w -d --define process.env.NODE_ENV='\"dev\"'",
    "build": "NODE_ENV='production' webpack -p --progress --optimize-minimize",
    "prestart": "echo antes",
    "server": "node server.js"
  },
  "devDependencies": {
    "babel-core": "^6.17.0",
    "babel-loader": "^6.2.5",
    "babel-preset-es2015": "^6.16.0",
    "browser-sync": "^2.17.5",
    "jquery": "^3.1.1",
    "webpack": "^1.13.2"
  }
}
